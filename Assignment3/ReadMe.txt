Overload the funtion:
  void insert (Data d, unsigned position);
This function will insert the data in front of the queue position.
If position is 0, it will insert at the front of that queue.
If position is size()-1, it will insert just in front of the tail.

Test Function
  bool testQueueEquality( Queue& q, vector<Data> const array);
take from professor
test if the queue equal to the Data set